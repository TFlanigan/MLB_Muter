#pragma once

// -----------------------------------------------------------------------------
// CREDENTIALS TEMPLATE
//
// 1. Copy this file in the same folder and rename the copy to "secrets.h".
// 2. Fill in your own values below.
//
// "secrets.h" is listed in .gitignore, so your real credentials stay on your
// machine and are never committed or pushed to GitHub.
// -----------------------------------------------------------------------------

const char* WIFI_SSID  = "YOUR_WIFI_SSID";
const char* WIFI_PASS  = "YOUR_WIFI_PASSWORD";
const char* EI_API_KEY = "YOUR_EDGE_IMPULSE_API_KEY";  // Edge Impulse API key (only needed for data_collection)
